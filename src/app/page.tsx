/**
 * Root-Seite.
 * Die Weiterleitung (→ /dashboard oder → /login) übernimmt
 * die Middleware in src/middleware.ts – hier kein redirect() nötig.
 */
export default function RootPage() {
  return null;
}
